{
  "name": "altery-eligibility-checker",
  "version": "1.0.0",
  "private": true,
  "description": "Altery pre-onboarding eligibility checker prototype with Stripe Payment Element",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "stripe": "^17.5.0"
  }
}
