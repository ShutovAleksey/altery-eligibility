{
  "name": "altery-eligibility-checker",
  "version": "1.0.0",
  "private": true,
  "description": "Altery pre-onboarding eligibility checker",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "verify:baselines": "node scripts/verify-baselines.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
